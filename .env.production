# ============================================================
# 生产模式（npm run build:mp-weixin 等 build:* 命令）—— 自动生效，无需手动切换
# ------------------------------------------------------------
# 指向线上后端：nginx 反代 https://wellwin.top/guanzhi → 127.0.0.1:3002
#
# 两条硬性约束（vite.config.ts 会校验，不满足直接构建失败）：
#   1. 必须是 https —— 小程序真机强制 https，http 会被拦截；
#   2. 不得是本机地址 —— 防呆：避免把「连 127.0.0.1」的包上传成体验版。
#
# 上线前确认（微信公众平台 → 开发管理 → 开发设置 → 服务器域名）：
#   request 合法域名      https://wellwin.top
#   downloadFile 合法域名 https://wellwin.top   （背景图走这条）
# ============================================================
VITE_API_BASE_URL=https://wellwin.top/guanzhi
