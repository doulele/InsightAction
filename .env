# ============================================================
# 观止知行 · 环境变量（通用层）
# ------------------------------------------------------------
# 本文件只放「各环境通用」的变量。
# 与环境相关的值（接口地址）一律写到分模式文件里，不要在这里手动切换：
#
#   .env.development → npm run dev:*    （本地联调：请求本机后端 3002）
#   .env.production  → npm run build:*  （打包上传：请求线上 wellwin.top）
#
# Vite 加载顺序：.env → .env.[mode] → .env.[mode].local（后者覆盖前者）
# 所以接口地址不要写在 .env 里，否则会「看起来配了但被覆盖/覆盖不掉」。
#
# 构建期守卫见 vite.config.ts：build 时会校验接口地址（必须 https、不得指向本机），
# 配错直接构建失败，不会让你把连本机的包传上线。
# ============================================================

# 横幅图兜底前缀（必须以 / 结尾），仅在后端接口不可用时使用。
# 正常留空：图片地址由后端 GET /guanzhi/skins 下发（含 ?v= 版本号）。
VITE_SKIN_BASE_URL=
