# 观止知行 · 隐私保护指引 · 未来功能预案

> ⚠️ **本文档任何时候都不得整篇提交**
>
> 提交审核只用 `PRIVACY.md`（已如实描述当前已实现的行为）。
> 本文只存放**尚未实现**的功能在将来启用时，需要往 `PRIVACY.md` 里补充的条款文本。
>
> 平台会把你填写的内容与代码实际行为比对 —— **声明了却没做，同样构成不实声明**，
> 所以未实现的功能绝不能提前写进提交版。启用时才插入，插完先在代码里跑通再提审。
>
> **已于 2026-09-15 实现并写入 `PRIVACY.md`**（不再是预案）：
> 云备份（openid + 云端备份内容）、AI 能力（你写的正文发往第三方服务商 DeepSeek）、
> 头像昵称填写能力（`chooseAvatar` + `type="nickname"`，见 `PRIVACY.md` 第 3 节）、
> 微信运动步数（`wx.getWeRunData`，见 `PRIVACY.md` 第 4 节；服务端只解密、不留存）。
>
> **已于 2026-09-16 实现并写入 `PRIVACY.md`**（不再是预案）：
> 语音转文字（「费曼速记」的「按住说话」，见 `PRIVACY.md` 第 5 节与「二、第三方插件信息」）——
> 走微信官方「微信同声传译」插件，**录音在微信侧识别、不经过我们的服务器**，
> 我们不读取也不保存录音文件；服务器只在你随后点「费曼检验」时才收到识别的文字。
>
> **已于 2026-09-17 实现并写入 `PRIVACY.md`**（不再是预案）：
> 静修声音（禅定沙漏 / 静心茶室的循环环境音、开始与结束的一记提示音、呼吸干预的吸·屏·呼引导音）——
> 音频素材从我们自己的服务器（`wellwin.top`）**按文件名下载**，不含任何身份标识，
> **不涉及任何微信隐私接口、不读取麦克风、不上传任何与你相关的内容**；
> 下载的音频缓存在小程序私有目录（见 `PRIVACY.md` 第 11 节与第五节）。
>
> | 功能 | 需补充的条款 | 需同步的代码动作 | 后台勾选变化 |
> | --- | --- | --- | --- |
> | 用户反馈 | 见下「用户反馈内容」 | 提交前明示同意；表单不得强制收集联系方式 | 无（不走微信隐私接口） |
> | 匿名统计 | 见下「匿名聚合统计数据」 | 不得携带 openid / 设备号，保持真正匿名 | 无 |
> | 微信运动 | 已启用，见 `PRIVACY.md` 第 4 节 | 后端 `services/wxSession.js` 留存 session_key + `routes/werun.js` 解密；步数只落本机且**不含于云备份** | 已勾选「收集你的微信运动步数」；提审前仍需确认服务类目能否覆盖运动/健康场景 |
> | 语音转写 | 已启用，见 `PRIVACY.md` 第 5 节 | `manifest.json → mp-weixin.plugins` 声明 `WechatSI`；`utils/voice.ts` 只取文字、不读录音文件；后台「插件管理」需添加 `wx069ba97219f66d99` | 需勾选「**麦克风**」；被拒时页面静默降级为手打字，不影响其它功能 |
> | 静修声音 | 已启用，见 `PRIVACY.md` 第 11 节与第五节 | `config/audio.ts` 记素材文件名、`utils/audio.ts` 只按文件名取音频并缓存进私有目录；不请求任何隐私接口、不读麦克风 | 无（不走微信隐私接口，后台**不需要**新增任何勾选） |
>
> 提醒：后台「开发者处理的信息」**永远不要**勾"未采集隐私"（会导致已勾选接口权限被回收）。

---

以下为备用条款文本。启用对应功能时，把它们**插入** `PRIVACY.md` 的「一、我们处理的信息与处理目的」，
并在「三、第三方服务商信息」「四、用户权益」「五、我们如何存储信息」同步补上对应条目。

### 用户反馈内容 —— 仅在你主动提交反馈时处理

- **触发场景**：仅当你**主动**提交意见反馈时；
- **处理内容**：你在反馈表单中填写的文字内容，以及（**可选**）你自愿留下的联系方式（如邮箱）；
- **处理目的**：用于了解问题、改进产品，并在必要时回复你；
- **处理方式**：仅存储于我们的服务器供开发者本人查看，不会公开，也不用于广告推送。你可以选择不填写任何联系方式。

### 匿名聚合统计数据 —— 仅在使用过程中自动记录

- **触发场景**：使用本小程序过程中自动记录；
- **处理内容**：**仅聚合计数**，例如功能打开次数、完成一段静修的次数、次日是否再次使用等。**不含** openid、昵称头像、手机号、设备号、位置、通讯录等任何可识别到你个人的信息；
- **处理目的**：了解功能是否有人使用、哪一步有人流失，用于改进产品；
- **处理方式**：仅存储聚合后的数值结果，不建立个人行为档案，无法回溯到具体用户。

> 微信运动的备用条款已于 2026-09-15 并入 `PRIVACY.md` 第 4 节（功能已上线），此处不再保留副本，
> 以免两处措辞漂移。**此后若调整其处理方式（例如改为云端留存），记得同步改一节的正文、
> 一节的存储表，以及后台勾选。**
