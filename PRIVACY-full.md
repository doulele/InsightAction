# 观止知行 · 隐私保护指引 · 未来功能预案

> ⚠️ **本文档任何时候都不得整篇提交**
>
> 提交审核只用 `PRIVACY.md`（已如实描述当前已实现的行为）。
> 本文只存放**尚未实现**的功能在将来启用时，需要往 `PRIVACY.md` 里补充的条款文本。
>
> 平台会把你填写的内容与代码实际行为比对 —— **声明了却没做，同样构成不实声明**，
> 所以未实现的功能绝不能提前写进提交版。启用时才插入，插完先在代码里跑通再提审。
>
> **已于 2026-09-15 实现并写入 `PRIVACY.md`**（不再是预案）：
> 云备份（openid + 云端备份内容）、AI 能力（你写的正文发往第三方服务商 DeepSeek）、
> 头像昵称填写能力（`chooseAvatar` + `type="nickname"`，见 `PRIVACY.md` 第 3 节）、
> 微信运动步数（`wx.getWeRunData`，见 `PRIVACY.md` 第 4 节；服务端只解密、不留存；
> 首次需主动授权，授权后隔几天进「行」大厅自动续接一次，未授权则不读取）。
>
> **已于 2026-09-16 实现并写入 `PRIVACY.md`**（不再是预案）：
> 语音转文字（「费曼速记」的「按住说话」，见 `PRIVACY.md` 第 5 节与「二、第三方插件信息」）——
> 走微信官方「微信同声传译」插件，**录音在微信侧识别、不经过我们的服务器**，
> 我们不读取也不保存录音文件；服务器只在你随后点「费曼检验」时才收到识别的文字。
>
> **已于 2026-09-17 实现并写入 `PRIVACY.md`**（不再是预案）：
> 静修声音（禅定沙漏 / 静心茶室的循环环境音、开始与结束的一记提示音、呼吸干预的吸·屏·呼引导音、
> 止念一刻写下或了结时的「落定」一声）——
> 音频素材从我们自己的服务器（`wellwin.top`）**按文件名下载**，不含任何身份标识，
> **不涉及任何微信隐私接口、不读取麦克风、不上传任何与你相关的内容**；
> 下载的音频缓存在小程序私有目录（见 `PRIVACY.md` 第 11 节与第五节）。
>
> **2026-09-19 补充（播放口径调整，网络访问范围未变）**：播放器改为**全程只用一个音频实例**，
> 音频优先读本机缓存，本地副本播不出来时才**从同一个地址流式播放**（同一个域名、同一个"按文件名取文件"的请求，
> 仍不携带任何身份标识）；`止念一刻` 加入同一套声音开关。
>
> **同日（第二批：日课 / 安息日 / 今日收功 / 旧卡重逢 / 时间胶囊 / 步数联动）—— 无需任何新条款**：
> 六项全部纯本地，**不新增任何微信隐私接口、不产生任何上行数据**（时间胶囊与步数一样只在本机，
> 已写进 `PRIVACY.md` 第 7 节与第五节存储表）。此处不保留副本，以免两处措辞漂移。
>
> **2026-09-19（第五批：收听换成服务端合成）—— 隐私面扩大，已并入 `PRIVACY.md`**：
> 「听」从"由微信同声传译插件在微信侧合成"改成**默认经我们的服务器转交第三方在线语音合成服务
> （微软 Edge 语音合成）**，因为插件一次只能合 50 字、只有一套发音，读长文是几十段拼接。
> 三条口径已写进 `PRIVACY.md` 第 5.2 节：「文字只在这一次请求的内存里（不落盘、不进数据库、不打日志）」、
> 「合成结果只在服务器**内存**里短暂保留供你当场下载，重启或淘汰即消失」、
> 「服务端不可用时**自动回落**到插件在微信侧合成」；同时在「三、第三方服务商信息」新增受托方
> （微软，在线语音合成服务）、第十一节网络访问表新增两行（`POST /guanzhi/tts`、`GET /guanzhi/tts/audio/…`）、
> 第五节存储表新增两行（合成的文字 / 合成的音频：内存 + 本机私有目录）。
> **后台勾选不变**（不涉及任何微信隐私接口），但**引导语与第 9 节的"数据何时离开设备"已同步改写** ——
> 这是本次唯一一条"数据出设备"性质的实质变化，后续若换回插件或换其他合成服务，都要照这条路径改。
>
> **2026-09-19（第四批：记一笔粘贴清单 / 契约按形态给 / 卡片摘要限高 / 正文收听）——
> 只有「正文收听」涉及新的数据处理，其余全部纯本地**（粘贴清单是在输入框里手动粘贴，
> **刻意不读剪贴板**，所以第 2 节"只写不读"的承诺仍然成立）：
> 收听（「听」）用**已经在用的**「微信同声传译」插件做**语音合成** ——
> 仅在你点击时，把你选择收听的那段正文文字按 ≤50 字分段交给插件，插件回一个 3 小时有效的音频地址，
> 当场播完即弃（我们不保存、不落盘、不进备份；插件偶发下成临时文件时播完即删）。
> **不涉及任何微信隐私接口、不录音、后台勾选不变**；性质与第 8 节的 AI 功能同类（你主动把某段文字交给第三方），
> 区别是不经过我们的服务器。已并入 `PRIVACY.md` 第 5 节（新增 5.2）、「二、第三方插件信息」、
> 第九节补充说明、第十一节结尾注与第五节存储表。此处不保留副本，以免两处措辞漂移。
>
> **2026-09-21（第六批：分享）—— 隐私面扩大，已并入 `PRIVACY.md`**：
> 「分享」把**你点的那一条**（标题 / 一句话总结 / 正文 / 你写的金句·观点·感悟·为什么成立 / 来源与原文链接）
> 存一份在我们的服务器上，生成一个**免登录可读**的链接（小程序卡片 + 网页版 `https://wellwin.top/guanzhi/s/<token>`）。
> 三条口径已写进 `PRIVACY.md` **第 12 节**（新增一节）：① **只有那一条**，不含身份、昵称、头像，也不是整份数据；
> ② **默认 30 天后自动删除**，随时可撤回、撤回即刻删除，且**与云备份完全分开**（不同目录、互不读取，
> 关闭云备份不影响已发出的链接）；③ **任何拿到链接的人都能看** —— 这是分享的本意，所以发布前必有一次确认。
> 同时改了四处：第十一节网络访问表 +2 行、第五节存储期限表 +1 行、「三、第三方服务商信息」（阿里云一行补注）、
> 引导语与第六节；**并顺带修正一处老说法** —— 第 6 节原写"openid 仅在你开启云备份后才会处理"，
> 而 AI / 步数解密 / 上传解析 / 收听合成早就在用登录态了，现改为"你主动使用需要携带登录凭据的功能时"。
> **后台勾选不变**（分享不调用任何微信隐私接口）；**无新增域名**（沿用 `wellwin.top`）。
>
> **2026-09-22（第七批：语音转写改走后端）—— 隐私面扩大，已并入 `PRIVACY.md`**：
> 起因是**审核驳回**：微信同声传译插件在服务市场的类目是「IT科技-软件服务提供商」，而《小程序插件功能介绍》
> 要求"插件类目不能超过使用方小程序主体类型当前开放的范围" —— **个人主体没有 IT科技 类目**，
> 带着这个插件提审必被驳回（2026-09-22 实测）。
> 所以新增一条**服务端转写**通道（`POST /guanzhi/asr`）：插件不可用时，用 `uni.getRecorderManager`
> 录 mp3（≤60 秒）→ 读成 base64 → 上传到我们的服务器转成文字。
> 口径已写进 `PRIVACY.md` **第 5.1 节**（该节改写成"两条通道"）：录音只在**这一次请求的内存**里处理，
> **不落盘、不进数据库、不打日志**（日志只记音频字节数 / 时长 / 耗时 / 转出字数），识别完即释放；
> 默认识别程序运行在**我们服务器本机**（离线模型），音频**不交给第三方** ——
> 若将来改用第三方在线识别服务，须**先更新「三、第三方服务商信息」与第 5.1 节、并重新取得你的同意**。
> 同时改了四处：第十一节网络访问表 +1 行、第五节存储表 +1 行、引导语与第 9 节的"数据何时离开设备"、
> 「二、第三方插件信息」补一条"插件不可用时不走它"的说明。
> **后台勾选不变**（仍是同一个「麦克风」能力，只是识别发生在哪一侧不同）；**无新增域名**。
>
> **2026-09-23（第八批：AI 开通状态核验）—— 网络访问多一条，已并入 `PRIVACY.md`**：
> 起因是一个"只说一半"的体验问题：设置页的「AI 授权」开关此前**只写本机的同意标记**，
> 而后端还有一道 `AI_ALLOWED_OPENIDS` 白名单 —— 不在名单里会返回 403，前端把它归进"静默兜底"，
> 于是用户看到的是"AI 功能看着有、出来的永远是基础结果，且没有任何解释"。
> 现在新增一条**开通状态核验**：点开「设置 → AI 授权」的开关时，先弹本机同意（内容会离开设备），
> 再向 `GET /guanzhi/ai/access` 问一次"我的账号能不能用"（**一次请求**，只回"能用 / 需要开通"，
> **不返回任何内容、也不透露服务端有没有配 Key**）；不在名单就不打开开关，弹一张二维码让用户找开发者开通。
> 已并入 `PRIVACY.md` **第 11 节网络访问表 +1 行**、**第 8 节**（改写为"前置同意与开通核验"）、
> **第 6 节**（openid 的触发场景补"核验 AI 是否已开通"）；并顺带把第 8 节"本地规则出的标注「本地」"
> 更正为「**基础**」（对外口径早已统一为"基础"，是文档滞后）。
> **后台勾选不变**（不调用任何微信隐私接口）；**无新增域名**。
>
> **2026-09-23（第九批：AI 白名单管理面）—— 存储面扩大一点，已并入 `PRIVACY.md`**：
> 缘由是"加人太麻烦"（原本要改 `.env` + 重启进程），而普通用户**拿不到自己的 openid**（要装开发者工具看网络面板）。
> 所以加了「申请开通」通道：用户在小程序里点一下，他的微信标识与**自己填的一句称呼**上报到我们服务器
> （`POST /guanzhi/ai/apply`），据此人工开通；白名单改落 `data/ai-allowlist.json`（改文件即生效，不必重启）。
> 已并入 `PRIVACY.md` **第五节存储表 +1 行**（开通名单与申请）、**第 11 节网络表 +1 行**、**第 8 节**（补"申请开通"一句）。
> **后台勾选不变**（不调用任何微信隐私接口）；**无新增域名**（仍是 `wellwin.top`）。
>
> **2026-09-17（第三批：步骤嵌套 / 两份正文 / 来源账本 / 知识库书架 / 记一笔导入文件）**：
> 除「导入文件」外全部纯本地。「导入文件」复用**已有的** `wx.chooseMessageFile`
> （此前只用于恢复备份），但分成两条路，性质不同：
>  · **文本清单（`.json / .md / .txt`）：本机解析**，不上传、不留存；
>  · **Word / Excel / PDF / 图片：上传到服务器解析**（后端 `POST /guanzhi/parse`）——
>    文件只在这一次请求的内存里，**不落盘、不进数据库、不打日志，解析完即删**；
>    其中图片转交微信官方「通用印刷体识别」接口；**上传前小程序会单独弹一次确认**。
> 已并入 `PRIVACY.md` 第 1 节（触发场景 + 两种处理方式分开写）、第四节网络访问表与第五节存储表；
> 后台勾选**不变**（「收集你选中的文件」此前已勾，没有新增任何隐私接口）。
> 此处不保留副本，以免两处措辞漂移。
>
> **2026-09-23（第九批：界面与交互收拾 · 稍后读扩成三种碎片形态）—— 无需任何新条款**：
> 「稍后读」从"只能存一句话"扩成三种形态（一句话 / 文章 / 视频），文章与视频多存一个**标题与链接** ——
> 与收件匣 / 详情页里已有的"原文链接 / 视频链接"同类，**只写在本机存储**，口径与云备份第 7 节一致
> （仍随你开启的云备份一起走，不新增任何上行）。
> 页面上新增的"点一下复制链接"走的是**已有的** `wx.setClipboardData`（`PRIVACY.md` 第 2 节：
> 仅写入、绝不读取你剪贴板里已有的内容），已在第 2 节的触发场景里补上这一例。
> **不新增任何微信隐私接口、不新增域名、后台勾选不变**；同批其余改动（输入弹框改用主题组件、
> 卡片与筛选行排版、入口行的 tab 指向修正、标志物与引文块样式）全部是纯视觉。
>
> | 功能 | 需补充的条款 | 需同步的代码动作 | 后台勾选变化 |
> | --- | --- | --- | --- |
> | 用户反馈 | 见下「用户反馈内容」 | 提交前明示同意；表单不得强制收集联系方式 | 无（不走微信隐私接口） |
> | 匿名统计 | 见下「匿名聚合统计数据」 | 不得携带 openid / 设备号，保持真正匿名 | 无 |
> | 微信运动 | 已启用，见 `PRIVACY.md` 第 4 节（**2026-09-21 起：首次需主动授权，之后自动续接**） | 后端 `services/wxSession.js` 留存 session_key + `routes/werun.js` 解密；步数只落本机且**不含于云备份**；前端 `utils/werun.ts` 的 `autoSyncWeRun()` 挂在 `pages/action` 的 `onShow`，判定在 `stores/body.ts` 的 `canAutoSync`（已授权 + 数据过期 ≥2 天 + 距上次尝试 ≥20 小时 + 非 23:00–06:00，连续 2 次无进展即停止自动） | 已勾选「收集你的微信运动步数」；提审前仍需确认服务类目能否覆盖运动/健康场景；**自动续接不涉及新勾选**（仍是同一个接口，只是触发时机多了一个） |
> | 语音转写 | 已启用，见 `PRIVACY.md` 第 5.1 节（**2026-09-22 起为两条通道**） | **首选插件**：`manifest.json → mp-weixin.plugins` 声明 `WechatSI`，`utils/voice.ts` 只取文字、不读录音文件；**回退服务端**：`uni.getRecorderManager` 录 mp3 → `POST /guanzhi/asr`（后端 `services/asrService.js`，`ASR_PROVIDER` = local 本机 faster-whisper / http 第三方 OpenAI 兼容接口 / off；录音经 base64 上传，只在内存里过一手）。**提审前要删掉 `manifest.json` 的 plugins 块**（插件类目对个人主体超范围，留着必被驳回）；顺序、录音参数与文案在 `config/voice.ts` | 需勾选「**麦克风**」（两条通道共用）；两个引擎都不可用时页面整条收起按钮，仍可手打 |
> | 语音合成（收听） | 已启用，见 `PRIVACY.md` 第 5.2 节、「二、第三方插件信息」与「三、第三方服务商信息」 | **默认走服务端**：`utils/speech.ts` → `POST /guanzhi/tts`（≤1200 字一段）→ 后端 `services/edgeTts.js` 调微软在线合成 → 音频下载进本机私有目录（`gz-tts-*.mp3`，第二次零请求）；**回落**：远端 `features.tts=false` 或服务端第一段就不通时，改用插件 `textToSpeech`（50 字一段）；`utils/audio.ts` 借出唯一的播放实例；阈值与文案在 `config/speech.ts` | **无**（不走任何微信隐私接口，后台**不需要**新增勾选） |
> | 静修声音 | 已启用，见 `PRIVACY.md` 第 11 节与第五节 | `config/audio.ts` 记素材文件名、`utils/audio.ts` 只按文件名取音频（优先本机缓存，播不出来时才从同一地址流式播放）并缓存进私有目录；四处接入点为沙漏 / 茶室 / 呼吸干预 / 止念一刻；不请求任何隐私接口、不读麦克风 | 无（不走微信隐私接口，后台**不需要**新增任何勾选） |
> | **分享（一条内容）** | 已启用，见 `PRIVACY.md` **第 12 节**（另见第 11 节网络表 2 行与第五节存储表 1 行） | 后端 `services/shareStore.js`（**单独一份** `data/shares/<token>.json`，与 `data/snapshots` 分开、互不读取；服务端**自己洗一遍 HTML**；30 天到期巡检删除；撤回比对 ownerHash）/ `services/sharePage.js`（H5 **零脚本 + 零外部资源 + 严格 CSP + no-store**）/ `routes/share.js`（发布·撤回需登录，**读取免登录**）；前端 `subpkg-observe/share` 只读页（免登录）、详情页面板（**发布前必有一次确认**）、`stores/share.ts` 留痕（进云备份）、**重置数据前先 `revokeAll()`**；`App.vue` 从分享卡片进来不走启动页；**token 不进日志**（node 打码 + nginx `access_log off`） | **无**（不走任何微信隐私接口，后台**不需要**新增勾选）；也无新增域名 |
>
> 提醒：后台「开发者处理的信息」**永远不要**勾"未采集隐私"（会导致已勾选接口权限被回收）。

---

以下为备用条款文本。启用对应功能时，把它们**插入** `PRIVACY.md` 的「一、我们处理的信息与处理目的」，
并在「三、第三方服务商信息」「四、用户权益」「五、我们如何存储信息」同步补上对应条目。

### 用户反馈内容 —— **已于 2026-09-23 实现并写入 `PRIVACY.md` 第 13 节**

实现形态与预案不同，故这里只留一句指路：它不是"反馈表单 + 可选联系方式"，而是「回音壁」
（更新 / 路线 / 回音 三段）—— **内容先审后发**（新写的先是只有作者与开发者可见，放行后才公开）、
**身份只存不可逆哈希（不保存微信标识原文）**、**文字会先交给微信官方内容安全接口检测一次**、
**作者随时可撤回**（撤回即从服务器删除）。完整条款见 `PRIVACY.md` 第 13 节
（第 11 节网络表 2 行、第五节存储表 2 行、第四节权益表 1 行均已同步）。
此处不再保留副本，以免两处措辞漂移。

### 匿名聚合统计数据 —— 仅在使用过程中自动记录

- **触发场景**：使用本小程序过程中自动记录；
- **处理内容**：**仅聚合计数**，例如功能打开次数、完成一段静修的次数、次日是否再次使用等。**不含** openid、昵称头像、手机号、设备号、位置、通讯录等任何可识别到你个人的信息；
- **处理目的**：了解功能是否有人使用、哪一步有人流失，用于改进产品；
- **处理方式**：仅存储聚合后的数值结果，不建立个人行为档案，无法回溯到具体用户。

### 「记一笔」导入二进制文件（Word / Excel / PDF / 图片）—— **已于 2026-09-18 实现并写入 `PRIVACY.md`**

本条按原计划落地（不再是预案）：后端 `POST /guanzhi/parse`（`InsightActionBacend/routes/parse.js`
+ `services/parseService.js`）解析，**文件只在这一次请求的内存里，解析完即删，不落盘、不打日志**；
图片转交微信官方「通用印刷体识别」；小程序侧在**上传前单独弹一次确认**。
`PRIVACY.md` 第 1 节（两种处理方式分开写）、第四节网络访问表、第五节存储表均已同步。
此处不再保留副本，以免两处措辞漂移。

> 微信运动的备用条款已于 2026-09-15 并入 `PRIVACY.md` 第 4 节（功能已上线），此处不再保留副本，
> 以免两处措辞漂移。**此后若调整其处理方式（例如改为云端留存），记得同步改一节的正文、
> 一节的存储表，以及后台勾选。**
