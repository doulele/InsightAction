# ============================================================
# 开发模式（npm run dev:*）—— 自动生效，无需手动切换
# ------------------------------------------------------------
# 本机后端：InsightActionBacend（在该目录 npm run dev，监听 3002）
#
# ⚠️ 微信小程序端注意（实测 uni-app 3.0.0-5020420260813003 + vite 5.2.8）：
#   mp 的开发编译【不会注入 import.meta.env】，
#   dist/dev/mp-weixin/config/env.js 里是 `define_import_meta_env_default = {}`，
#   所以本文件对 mp-weixin 开发态不起作用 —— 微信小程序的开发态地址
#   由 src/config/env.ts 里的 DEV_API_BASE_FALLBACK 常量决定（同样是本机后端）。
#   本文件对 H5（npm run dev:h5）等走 Vite 原生流程的平台有效。
#
# 两条约束（两端都要满足）：
#   1. src/config/env.ts → DEV_API_BASE_FALLBACK = http://127.0.0.1:3002/guanzhi
#   2. 微信开发者工具 →「详情 → 本地设置 → 不校验合法域名、web-view、TLS 版本以及 HTTPS 证书」
#      （http://127.0.0.1 必须关闭域名校验，真机预览连不上本机，联调请用开发者工具）
# ============================================================
VITE_API_BASE_URL=http://127.0.0.1:3002/guanzhi
