# 观止知行 · 小程序用户隐私保护指引

> **使用说明（提交审核前请删掉这一段）**
> 1. 占位信息已全部填完（主体名称、联系邮箱、日期、两家受托方主体全称）；
> 2. 本指引已与**代码实际行为**逐条核对（最近一次复核 2026-09-21）：
>    - 微信隐私接口只用了五处：`wx.setClipboardData`（仅写入剪贴板）、
>      `wx.chooseMessageFile`（选备份文件 / 选「记一笔」要导入的文本清单）、头像昵称填写能力（`chooseAvatar` +
>      `type="nickname"` 输入框）、`wx.getWeRunData`（读微信运动步数，**首次需你点击
>      「同步今日步数」授权；授权后，在步数数据过期时会自动续接一次，详见第 4 节**）、**麦克风录音**（「按住说话」，**仅在你按住按钮时**，
>      由微信同声传译插件在微信侧识别，录音不经过我们的服务器）；
>      **没有**读取剪贴板、相册、摄像头、位置、通讯录、手机号、传感器等任何其他隐私接口；
>    - 已如实披露：微信标识（openid，仅开云备份时）、云端备份内容（仅开云备份时）、
>      AI 功能中你写的正文会发给第三方服务商（深度求索 DeepSeek，仅你授权使用时）、
>      微信运动步数（由我们的服务器解开微信的密文后即返回，**服务端不留存**）、
>      语音转文字（音频在微信侧识别，我们只收文字）、
>      **语音合成（「听」：仅在你点击时，把你选择收听的那段正文经我们的服务器转交第三方在线合成服务
>      —— 微软 Edge 语音合成 —— 生成音频；服务端只在内存里过一手、不落盘不打日志，
>      合成结果下载到本机后同一段不再请求。服务端不可用时自动回落到微信同声传译插件在微信侧合成。
>      **不涉及任何微信隐私接口**）、
>      **你主动分享的那一条内容**（仅在你点击某条详情页的「分享」时，把**那一条**上传到我们的服务器
>      生成分享链接；默认 **30 天**后自动删除，你可随时撤回、撤回即刻删除；见第 12 节）**。
> 3. 微信后台「开发者处理的信息」需勾选五项：**收集你的微信昵称、头像**、
>    **读取你的剪切板**、**收集你选中的文件**、**收集你的微信运动步数**、
>    **麦克风**；其余一律不勾，
>    也**不要**勾"未采集隐私"（勾了会导致这些接口权限被回收）。
>    openid 与云备份、AI 属于我们自行处理的信息，在本文本中声明即可，不在该清单内。
>    **语音合成（「听」）不新增任何勾选** —— 它不调用隐私接口（见第 5 节）。
> 4. 插件相关：小程序后台「设置 → 第三方服务 → 插件管理」需添加「微信同声传译」
>    （AppID `wx069ba97219f66d99`），否则「按住说话」与「听」都不会出现（页面静默降级，不报错）。
> 5. DeepSeek 主体全称已核实为「杭州深度求索人工智能基础技术研究有限公司」，可直接使用；
>    云服务托管方请按你的服务器实际厂商填写其公司全称。

**更新日期：2026 年 9 月 21 日**
**生效日期：2026 年 9 月 21 日**

---

## 引导语

观止知行（以下简称"本小程序"）由窦乐园（以下简称"我们"）开发并运营。

我们深知个人信息对你的重要性，并本着"**最小必要、本地优先、默认不出设备**"的原则处理你的信息。

本小程序的核心设计是：**你的修行数据默认只存在你自己手机的本地存储中，我们无法访问**。只有下面这几种情况会有数据离开你的设备，且都由你**主动**触发：**主动**开启云备份、**主动**使用 AI 功能、**主动**点击正文旁的「听」收听（把那段正文交给语音合成服务）、**主动**点击详情页的「分享」（把**那一条**生成分享链接）、**主动**点「同步今日步数」（**首次授权后会自动续接**，见第 4 节）、**主动**导入需要上传解析的大文件（Word / Excel / PDF / 图片）。它们都写在下文第 1～12 节里；云备份与 AI **默认关闭**、可随时撤回，其余都是"点一下才发生一次"，你不点就不会有任何内容被提交或上传。

请你在使用本小程序前仔细阅读并充分理解本指引。你开始使用本小程序，即表示你已同意本指引的内容。

---

## 一、我们处理的信息与处理目的

### 1. 你选中的文件（对应接口：`wx.chooseMessageFile`）

- **触发场景**：仅在你**主动**使用以下两处时 —— ①「设置 → 数据 → 从备份恢复」；②「观 · 记一笔 → 导入文件」；
- **处理内容**：读取你从微信聊天记录中**主动选中**的那一个文件 —— 备份文件（`.json`）、要导入填表的**文本清单**（`.json` / `.md` / `.txt`），或 **Word（.docx）/ Excel（.xlsx）/ PDF / 图片**；
- **处理目的**：备份文件用于换机迁移或误删恢复；其余用于把里面的标题、正文等内容**填进你自己正在写的表单**（只是预填，存不存由你决定）；
- **处理方式（分两种，性质不同，请留意）**：
  1. **文本清单与备份文件：全过程仅在你的设备本机完成** —— 不上传、不留存该文件的任何副本，也不会读取文件之外的其他内容；
  2. **Word / Excel / PDF / 图片：这类文件你的手机读不了，需要上传到我们的服务器解析**。
     我们**只在这一次请求的内存里**处理它：**不落盘、不进数据库、不打日志，解析完成即删除**，
     不用于其他用途、不与你的身份关联；其中**图片会被转交微信官方「通用印刷体识别」接口**
     完成文字识别（见「二、第三方」）。**上传之前会单独弹一次确认**，你可以选择不上传 ——
     不给文件也有办法：直接手打，或让对方输出 `.json` / `.md` 文本，那样全程不离开手机；
- **你的控制**：随时可以取消选择；已导入的内容只是填在表单里，不点「存入收件匣」就不会保存任何东西。

### 2. 你的剪切板（仅写入，对应接口：`wx.setClipboardData`）

- **触发场景**：仅当你**主动**点击复制类功能（如「导出今日概览」「复制文字卡片」「复制文案」等）时；
- **处理内容**：把你当日的修行摘要或卡片文字**写入**系统剪贴板；
- **处理目的**：便于你自行粘贴到备忘录、聊天窗口等处保存或分享；
- **处理方式**：**仅写入，绝不读取你剪贴板中已有的任何内容**；写入内容不离开你的设备，我们不获取、不上传。

### 3. 你的头像与昵称 —— 仅在你主动设置时才会处理

- **触发场景**：仅当你**主动**点击「我 · 修行看板」头部右侧的「起号 / 修改」，进入编辑后再点选头像 / 填写昵称时（进入编辑前会先申请一次隐私授权 —— 微信规定昵称输入框须已获授权才提供「一键填入微信昵称」，未授权时它自动降级为普通输入框，你手动输入同样可用，不影响任何功能）；
- **处理内容**：你**自己选的**那张头像图片，以及你**自己填的**昵称文字（微信的「头像昵称填写能力」只在你点击时才把内容给到本小程序，我们无法自行调取）；
- **处理目的**：展示在你本机「我 · 修行看板」的个人信息区，让修行记录有个属于你的名号；
- **处理方式**：图片**仅保存在你手机的私有目录**中，与修行数据放在一起；**不上传服务器**。你随时可以点「用回默认」把两者一并删除（头像文件同时从本机删除）；
- **唯一例外**：你主动开启云备份后，昵称与头像会随备份一同上传（见第 7 节），关闭云备份即一并删除。

### 4. 你的微信运动步数 —— 首次需你主动授权，之后会自动续接

- **触发场景**：
  1. **首次**：仅当你**主动**进入「行 · 身体电量」页并点击「同步今日步数」时。微信会弹出授权框，**你同意后**才会有下面这一条；
  2. **续接**：你已授权、且本机步数数据已过期（最新一条距今超过 2 天）时，进入「行」大厅会**自动**读取一次。**每天最多一次**，23:00–06:00 不读取，读取失败也不提示、不打扰；
  3. **不会自动的情形**：你从未授权、或拒绝过授权时，**不会**有任何自动读取；连续两次自动读取都没能拿到更新的数据时，自动续接会**自行停止**，恢复为"只在你点击时读取"；
- **处理内容**：微信运动里**最近约 30 天**的每日步数（该接口本身只提供这么多，我们取不到更早的历史）；
- **处理目的**：作为「身体电量」展示在你自己的设备上，给身体的活动量一个粗略参照；
- **处理方式**：微信返回的是**加密数据**，由我们的服务器解开后**立即把明文步数返回给你** —— 服务端**不留存、不落库、不打日志**；步数只写入你手机的本地存储。你**主动导出/恢复的本地备份文件**里会有它（那份文件始终在你自己手里），**云备份不含步数**；
- **不用于**：健康或医疗诊断、对外提供、广告推荐、诱导分享，也不做任何排名或攀比设计；
- **你的控制**：随时通过「设置 → 数据 → 重置全部修行数据」清空步数记录（「今日目标步数」属于本机偏好，会保留）。

### 5. 你的录音（麦克风）与语音合成（「听」）—— 仅在你主动按住说话 / 主动点「听」时才会处理

**5.1 录音（对应接口：麦克风；即「按住说话」的语音转文字）**

- **触发场景**：仅当你**主动按住**「知 · 知识库」里的「按住说话」按钮（费曼速记的语音转写）时。**不按住就不录音**，松开手指立即结束；本小程序**不存在**后台录音、静默录音或录音权限的预申请。首次使用时微信会弹出麦克风授权框，你可以拒绝，拒绝后仍可直接手打文字，功能不受影响；
- **处理内容**：你按住那段时间里说的内容；
- **处理目的**：把它转成文字，填进你自己正在写的转述正文里，供你修改后存成知识卡片；
- **处理方式（关键）**：语音识别由**微信官方提供的「微信同声传译」插件在你手机本地与微信侧完成**（见本指引第二部分）。录音**不经过我们的服务器**，我们也**不读取、不保存、不上传**任何录音文件（插件回传的录音临时文件我们连读都不读）；我们的服务器**只在你随后点击「费曼检验」时**才会收到识别后的**文字**（属第 8 节的情形，且需另行授权）；
- **不用于**：声纹识别、身份识别、情绪分析、广告推荐，也不与你的身份关联；
- **你的控制**：随时可在微信「设置 → 小程序 → 观止知行 → 麦克风」中关闭该权限；页面上不按按钮就不会有任何录音。

**5.2 语音合成（「听」：把你自己的一条正文读出来）**

- **触发场景**：仅当你**主动点击**某条内容详情里正文旁的「听」按钮时（文章 / 视频的正文，以及「一句话」那一句）。**你不点，就什么都不会发生**；
- **处理内容**：**你选择收听的那段正文文字**（你已经存在的笔记内容）。为了让朗读不断句，会按句切成若干段（**每段不超过 1200 字**）分别交去合成，**不涉及麦克风、不录音**；
- **处理目的**：把它合成为语音，读给你听（走路、闭眼、通勤时也能把这段内容过一遍）；
- **处理方式（关键，分两步）**：
  1. **文字经我们的服务器转交给第三方在线语音合成服务（微软 Edge 语音合成）生成音频**。我们的服务器**只在这一次请求的内存里**处理它：**不落盘、不进数据库、不打日志**（日志只记字数、音色、体积）；合成结果也只在**服务器内存**中短暂保留、供你当场下载，**进程重启或缓存淘汰即消失**；
  2. 音频下载到**你手机的私有目录**后播放（文件名是"音色 + 文字"的哈希）。**同一段再次收听不再请求服务器**，本质上是听本机文件；
- **回落到插件的情形**：服务端合成不可用、或我们关掉了这个功能时，会**自动改为**由「微信同声传译」插件在微信侧合成（那一路文字**不经我们的服务器**，见「二、第三方插件信息」）。两种方式都只在你点击「听」时才发生；
- **不用于**：语音克隆、声纹识别、内容分析、广告推荐，也不与你的身份关联（合成请求携带登录凭据**仅用于限流**；服务端缓存键是"音色 + 文字"的哈希，不含你的身份）；
- **你的控制**：不点「听」即不发生；朗读随时可以暂停或停止；本机缓存的音频随你**卸载小程序 / 清理小程序缓存**一并清除（不随云备份上传，重置修行数据也不会留下副本）。

> 5.1 与 5.2 都只在**你主动按下的那一刻**才发生；区别在方向与走向 ——
> 转写是"声音进来、文字留下"（音频不出微信），合成是"文字出去、声音回来"（默认经我们的服务器交给第三方合成）。
> 两者**都不带你的身份标识**，也都不进云端备份。

### 6. 你的微信账号标识（openid）—— 仅在你主动使用需要联网的功能时才会处理

- **触发场景**：仅当你**主动使用需要携带登录凭据的功能**时（对照第 11 节网络访问表里"是否携带身份标识 = 是"的各行）—— 开启云备份、使用 AI 能力、同步微信运动步数、导入需要上传解析的文件、点击「听」收听、点击「分享」。凭据由微信 `wx.login` **静默获取，不弹授权框、不需你输入任何信息**；
- **处理内容**：由微信生成的、仅在本小程序内有效的用户标识（openid）。**不包括**你的微信昵称、头像、性别、地区、手机号、好友关系等任何资料；
- **处理目的**：把你的备份数据与"你本人"对应起来，以便你在新设备上恢复自己的数据；不做身份识别，不用于其他用途；
- **处理方式**：据此签发一个**7 天有效**的登录凭据保存在你手机本地，**手机上不保存 openid 本身**；凭据过期后自动重新获取，你无需任何操作。
- **不开云备份，就不会发生本项处理。**

### 7. 你的云端备份内容 —— 仅在你开启云备份后才会处理

- **触发场景**（均以你已开启云备份为前提）：
  1. 你**主动**点击「立即备份」；
  2. 距上次备份超过 **24 小时**时，小程序启动后自动备份一次（后台静默进行，失败也不打扰你）；
- **处理内容**：你在本机产生的修行数据（今日三件事、专注与静修记录、知识卡片、习惯打卡、计划与日课、测评结果、徽章与修为、收藏与痕迹、每日收功，以及你设置的昵称与头像）。**其中不含你的身份信息，也不含你的微信运动步数**；**「时间胶囊」（你写给未来的自己的话）同样只留在本机，不随云备份上传**；
- **处理目的**：用于你在更换设备、清理缓存或重装后恢复数据；
- **处理方式**：通过加密传输（HTTPS）上传并存储在我们的服务器（中国大陆境内）。除用于恢复外不做任何分析，不用于广告推送。
- **你的控制**：关闭「云备份」开关即删除云端全部数据。

### 8. 你主动提交给 AI 的内容 —— 仅在你授权并使用 AI 功能时才会处理

- **触发场景**：仅当你**主动**点击 AI 相关功能（如知识卡片页的「AI 检验」「AI 打个标签」「AI 极简报」）时；
- **前置同意**：首次使用会**弹窗征得你的明示同意**。你不同意也能继续用 —— 此时改用**本机规则**计算，内容不联网、不出手机，功能照常可用；
- **处理内容**：你当时正在使用的那段**笔记正文、卡片文字或记录条目**（即你主动选择交给 AI 处理的那段内容）；
- **处理目的**：生成检验结论、标签建议或摘要，结果直接返回给你。AI 出的结果在界面上标注「AI」，本地规则出的标注「本地」；
- **处理方式**：内容经我们的服务器转交给 AI 服务商生成结果。**我们未授权服务商将该内容用于模型训练**，也不作其他用途。
- **你的控制**：在「设置 → AI 授权」中可随时撤回；撤回后所有 AI 功能改用本机规则。

### 9. 以下信息我们一概不收集

手机号、位置信息、相册、摄像头、通讯录、日历、蓝牙、发票信息、车牌号、加速计/陀螺仪等传感器信息，以及你剪贴板中的既有内容。

补充说明：你的头像与昵称只在**你主动设置**时获取（见第 3 节），我们不会在其它时机自行调取你的微信头像、昵称或好友关系；微信运动步数**首次需你主动点击授权**，授权后仅在步数数据过期时自动续接一次（见第 4 节）；麦克风只在**你主动按住「按住说话」**时启用，且录音不离开你的手机与微信（见第 5 节）；「听」只在你点击时把**你自己那条正文的文字**交去合成语音（默认经我们的服务器转交第三方合成服务，服务端只在内存里过一手），不读取任何其他内容（见第 5 节）。

本小程序**不投放广告**，**不接入**任何第三方统计 SDK 或广告 SDK，**不进行**任何形式的用户画像。

补充说明：我们会在**你的手机本机**读取设备基础信息（如系统版本、窗口尺寸），仅用于将界面正确适配你的屏幕，这部分信息**不上传、不存储**。

### 10. 你的修行数据保存在哪里

未开启云备份时，你产生的全部数据均保存在**你手机上本小程序的本地存储**中，我们无法访问、服务器侧没有副本。

因此请你知悉：

- 你可以通过「设置 → 数据 → 导出全部数据」导出全部数据自行备份或迁移；
- 你可以通过「设置 → 数据 → 重置今日三件事 / 重置全部修行数据」删除本机数据；
- **卸载小程序或清理小程序缓存会导致本机数据丢失**（我们也无法帮你找回），建议定期导出备份，或开启云备份。

### 11. 网络访问说明

本小程序会访问我们的服务器域名 `wellwin.top`，用途仅限以下几项：

| 用途 | 发生时 | 是否携带身份标识 |
| --- | --- | --- |
| 下发界面配置（背景图、状态栏文案、公告等）与每日内容 | 正常使用过程中自动发生 | **否**，不含任何与你身份相关的信息 |
| 云备份的上传与恢复 | 仅你开启云备份后 | 是（7 天有效凭据） |
| AI 功能的生成请求 | 仅你授权并使用时 | 是（7 天有效凭据） |
| 微信运动数据的解密 | 仅你点击「同步今日步数」时；已授权后，步数数据过期时进入「行」大厅自动续接（每天最多一次，23:00–06:00 不读取） | 是（7 天有效凭据，服务端只据它换取当次解密所需的会话） |
| 静修声音的素材下载（白噪音音频文件） | 仅在你开启「静修声音」并使用禅定沙漏 / 静心茶室 / 呼吸干预 / 止念一刻时 | **否**，只是按文件名取一段音频文件（优先用本机已缓存的副本，本地播不出来时才从同一地址流式播放），不含任何与你身份相关的信息 |
| 导入文件的服务端解析（Word / Excel / PDF / 图片） | 仅当你在「观 · 记一笔」导入这几类文件、**并确认上传后** | 是（7 天有效凭据）；文件内容解析完即删，不打日志 |
| 正文收听的语音合成（`POST /guanzhi/tts`） | 仅当你点击某条正文旁的「听」时（每段 ≤1200 字，按段请求） | 是（7 天有效凭据，**仅用于限流**）；文字只在内存里转交合成服务，不落盘、不打日志 |
| 合成音频的下载（`GET /guanzhi/tts/audio/…`） | 紧接上一步，仅在你点击「听」时；下载到本机后同一段不再请求 | 是（同上）；音频从服务器**内存**读出发给你 |

除此之外，本小程序不向任何第三方发起携带你数据的请求。

> 「按住说话」与「听」的交互方式是两种：**转写**不经过我们的服务器（音频在微信侧识别）；
> **收听（合成）默认经过我们的服务器**（上表最后两行），只有服务端不可用时才回落到插件在微信侧合成。
> 插件的音频地址由微信侧或合成服务给出，不在上表之内，也不携带你的身份标识。

### 12. 你主动分享的那一条内容 —— 仅在你点击「分享」时才会处理

**这是本小程序唯一"内容会公开可读"的功能，请留意。**

- **触发场景**：仅当你**主动**打开某一条（事 / 理 / 道）的详情页、点击「分享」、并在确认框里同意时。**没有开关能让它自动发生**，你不点就不会有任何内容上传；
- **处理内容**：**只有你选中的那一条** —— 标题、一句话总结（或摘要）、正文（含样式）、你写的金句 / 重要观点 / 感悟 / 「为什么成立」、来源与原文链接等字段。**不是**你的全部数据，也**不含**你的微信标识、昵称、头像或任何其他记录；
- **处理目的**：生成一个**免登录可读**的分享链接，让你把这一条发给好友（小程序卡片），或发到任何地方（网页链接）；
- **处理方式**：
  1. 这一条以**单独一份**存放在我们的服务器上（**不与你的云端备份放在一起，也不读取它**）；
  2. 链接标识是**随机、不可猜测、不可枚举**的一串字符 —— 我们**不提供**任何"浏览他人分享"的列表或搜索入口；
  3. **任何拿到这个链接的人都能打开查看**（这正是分享的目的）。所以点「分享」之前请你想一下：这一条里有没有你不希望别人看到的内容；
  4. 网页版在我们服务器上渲染为**纯文字页面**（不加载任何脚本，也不含任何统计代码）；
- **保存期限与删除**：默认 **30 天**后自动失效并删除；你可以在原详情页点「撤回」**立即删除**；也可以通过下方邮箱要求我们删除某一条分享；
- **你的控制**：不点「分享」即不发生；撤回或过期后链接即时失效（打开只显示"该分享已被撤回 / 已过期"）；
- **与云备份无关**：即使你**从未开启**云备份，分享照样可用；反过来，**关闭云备份不会影响**你已经发出的分享链接 —— 分享快照与云端备份是两份互不读取的数据。

> 一句话概括：**分享是你主动把"那一条"公开放到网上（30 天、可撤回）**，而不是把你本机的数据整体传上去。

---

## 二、第三方插件信息

本小程序使用了**微信官方提供的「微信同声传译」插件**（插件 AppID：`wx069ba97219f66d99`，由深圳市腾讯计算机系统有限公司提供），用于两件事 ——「费曼速记」的语音转文字，以及正文收听（「听」）的**回落**语音合成：

| 项目 | 说明 |
| --- | --- |
| 使用场景 | ① 仅当你**主动按住**「按住说话」按钮时；② 仅当你**主动点击**「听」按钮、且服务端合成不可用（或我们关掉了它）时 |
| 交给插件的内容 | ① 你当时说的那段话的**音频**；② 你选择收听的**那段正文文字** |
| 处理位置 | 都在**微信侧完成**（识别 / 合成）。**都不经过我们的服务器**：转写我们不读取、不保存、不上传录音文件（插件返回的录音临时文件我们连读都不读）；合成返回的音频地址我们只在内存里用到本次播放结束，**不落盘、不进备份** |
| 我们收到的 | ① 只有识别出的**文字**，随后写入你手机的本地存储；② 只有那个**音频地址**（3 小时有效），当场播完即弃 |
| 涉及的微信隐私接口 | 只有 ① 用到**麦克风**（首次会弹授权）；② **不涉及任何隐私接口**，也不弹授权框 |

除此之外，本小程序**未使用**任何其他第三方插件。

> **收听（「听」）默认不走这个插件**：默认由我们的服务器把你的正文转交给第三方在线语音合成服务（见第 5.2 节与「三、第三方服务商信息」），因为它的音色更自然、一次能合上千字。只有服务端那一路不可用时才会回落到本插件的合成，口径与后果都写在第 5.2 节。

---

## 三、第三方服务商信息

本小程序由开发者自行开发与运营，**未委托**第三方服务商开发。以下两家仅作为**受托处理方**，按我们的指示提供基础设施或技术服务，不得将相关数据用于自身目的：

| 受托方 | 角色 | 涉及的数据 | 地域 |
| --- | --- | --- | --- |
| 阿里云计算有限公司 | 服务器托管 | 你的云端备份数据（仅你开启云备份后）；收听时经服务器内存转交合成的正文（**不落盘**）；**你主动分享的那一条内容**（单独存放，默认 30 天、可撤回） | 中国大陆境内节点 |
| 杭州深度求索人工智能基础技术研究有限公司（DeepSeek） | AI 生成服务 | 你主动提交给 AI 的笔记正文（仅你授权并使用 AI 功能时） | 以其公开的服务地域为准 |
| 微软（Microsoft，Edge 在线语音合成服务） | 语音合成服务 | 你**主动点击「听」**时选择收听的那段正文文字（经我们的服务器转交，仅用于当次合成为语音） | 以其公开的服务地域为准 |

> 说明：微软这一项是**在线语音合成服务**（本项目按公开接口调用，未与其另行签署数据处理协议）。它的作用是"把文字读成声音"，不参与任何内容分析；你**不点「听」**就不会有这段文字被提交。

---

## 四、用户权益

你可以通过本小程序内的以下方式查阅、复制、更正和删除你的信息：

| 你的权利 | 行使方式 |
| --- | --- |
| 查阅 / 复制 / 导出 | 「设置 → 数据 → 导出全部数据」，导出你本机全部修行数据 |
| 更正 | 相关记录均可在对应功能页内直接编辑修改 |
| 删除本机数据 | 「设置 → 数据 → 重置今日三件事 / 重置全部修行数据」（含清空已读取的微信运动步数） |
| 清除头像与昵称 | 「我 · 修行看板」→ 点头部右侧「修改」（未起号时显示「起号」）→「用回默认」（头像文件同时从本机删除） |
| 删除云端备份 | 关闭「设置 → 云备份」开关，云端全部数据将被删除 |
| 撤回 AI 授权 | 关闭「设置 → AI 授权」开关，此后 AI 功能一律改用本机规则 |
| 注销 / 申请清除 | 通过下方邮箱提出申请，我们将在核验后删除与你相关的数据 |

如你对本指引或你的个人信息处理有任何疑问、意见、投诉或建议，可通过以下方式联系我们，我们将在收到后 15 个工作日内答复：

- **联系邮箱：1220736335@qq.com**

（关于授权撤回与要求删除的通用路径说明，由微信平台按本小程序实际调用的接口自动生成并展示。）

---

## 五、我们如何存储信息

| 信息类型 | 存储位置 | 存储期限 |
| --- | --- | --- |
| 剪贴板写入的内容 | 你的设备（系统剪贴板） | 由你自行管理，我们不存储 |
| 你选中的文本清单与备份文件 | 你的设备本机 | 本机解析，从不离开设备 |
| 你导入的 Word / Excel / PDF / 图片 | 仅在我们的服务器内存中 | 解析完成即释放，不落盘、不留存、不打日志 |
| 头像与昵称 | 你的设备（小程序私有目录）；开启云备份后随备份上传 | 保存至你「用回默认」，或关闭云备份（届时云端一并删除） |
| 登录凭据（7 天有效） | 你的设备本地 | 到期自动失效并重新获取 |
| 云端备份数据 | 我们的服务器（中国大陆境内） | 保存至你主动删除，或关闭云备份功能（届时删除全部云端数据） |
| **你主动分享的那一条内容** | 我们的服务器（中国大陆境内），**单独存放**，与云端备份互不读取 | 默认**发出后 30 天**自动删除；你随时可点「撤回」，撤回即可删除 |
| 你提交给 AI 的内容 | 经我们的服务器转发至 AI 服务商 | 仅用于当次生成并返回结果，我们不做留存 |
| 微信运动步数 | **你的设备本机**（服务端解密后立即丢弃，不留存、不打日志） | 保存至你执行「重置全部修行数据」，或卸载小程序 / 清理缓存 |
| 时间胶囊（你写给未来的自己的一句话） | **你的设备本机**（与步数同样**不随云备份上传**） | 保存至你执行「重置全部修行数据」，或卸载小程序 / 清理缓存 |
| 静修声音的音频缓存 | **你的设备本机**（小程序私有目录，仅在你开启「静修声音」并用到时才下载；止念一刻那一声轻响也走同一套） | 保存至你卸载小程序 / 清理缓存；关闭「静修声音」后不再新增（已下载的可随缓存一并清除） |
| 收听（「听」）合成的文字 | 经我们的服务器**内存**转交第三方合成服务（不落盘、不进数据库、不打日志） | 当次请求处理完即释放 |
| 收听（「听」）合成的音频 | ① 服务器**内存**（供你当场下载，重启或缓存淘汰即消失）；② 下载后存在**你的设备本机**（小程序私有目录 `gz-tts-*.mp3`） | 本机缓存保存至你卸载小程序 / 清理缓存；**不随云备份上传** |

安全措施：全站 HTTPS 加密传输；云端接口需凭登录凭据访问，服务端按 openid 隔离不同用户的数据；服务器访问权限仅限开发者本人，并遵循最小权限原则。

需要说明的是：**卸载小程序或清理缓存会导致本机数据丢失**，我们无法为你恢复，建议你定期导出备份。

---

## 六、信息的使用规则

我们仅在为实现本指引明示的功能所必需的范围内处理相关信息，不会将其用于其他目的，也不会用于广告推送、用户画像或商业分析。如需超出上述目的使用，我们会再次以显著方式告知你并取得你的明示同意。

其中云备份与 AI 两项**默认均为关闭状态**：

- 云备份需你**主动开启**（开启前会展示说明），关闭即删除云端全部数据；
- AI 功能需你**首次使用时明示同意**，撤回后改用本机规则；
- 「听」（语音合成）**没有常开开关**：它一次点击才发生一次，你不点就不会有任何正文被提交；
- 「分享」同样**没有开关**：它是"点一次、发一条"，那一条默认 30 天后自动删除、随时可撤回；
- 拒绝上述任何一项，均**不影响**本小程序其他功能的正常使用。

---

## 七、信息对外提供

我们不会向任何第三方共享、转让或公开披露你的信息，以下情形除外：

- 本指引「三、第三方服务商信息」所列受托方，仅按我们的指示提供基础设施或技术服务，且只能在本指引所述目的范围内接触相应数据；
- 法律法规规定的强制性要求。

如未来因功能调整确需对外提供，我们将以显著方式另行告知你信息的类型、目的及接收方，并取得你的明示同意。

---

## 八、未成年人保护

本小程序不主动收集未成年人的任何个人信息。若你是不满 14 周岁的儿童，请在监护人的指导下阅读本指引并使用本小程序；若监护人发现我们在未获同意的情况下处理了儿童信息，可通过上述联系方式与我们联系，我们会及时删除。

---

## 九、本指引的更新

我们可能会适时更新本指引。更新后，我们会在本小程序内以显著方式提示你。若更新涉及信息处理范围、目的或类型的实质性变化（例如新增数据出境或新的第三方接收方），我们会重新向你告知并征得你的明示同意。更新版本自公示载明之日起生效。

---

## 十、联系方式

- **开发者名称：窦乐园**
- **联系邮箱：1220736335@qq.com**

**更新日期：2026 年 9 月 21 日**
**生效日期：2026 年 9 月 21 日**
